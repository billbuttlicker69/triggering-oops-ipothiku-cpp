#include <iostream>
using namespace std;
void add(int a,int b)
{
    cout<<"sum"<<(a+b);
}
void add(double a,double b)
{
    cout<<"add"<<(a+b);
}
int main()
{
    add(01,12);
    add(1.6,3.2);
    return 0;
} 
